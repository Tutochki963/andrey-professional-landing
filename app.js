const root=document.documentElement;const toggle=document.getElementById('langToggle');function applyLang(lang){root.setAttribute('lang',lang);root.dataset.lang=lang;document.querySelectorAll('[data-ru][data-en]').forEach(el=>{el.textContent=el.dataset[lang];});toggle.textContent=lang==='ru'?'EN':'RU';localStorage.setItem('ag-lang',lang);}const saved=localStorage.getItem('ag-lang');const preferred=saved||((navigator.language||'').toLowerCase().startsWith('ru')?'ru':'en');applyLang(preferred);toggle.addEventListener('click',()=>applyLang(root.dataset.lang==='ru'?'en':'ru'));

const mobileFirstScreenStyles=document.createElement('link');mobileFirstScreenStyles.rel='stylesheet';mobileFirstScreenStyles.href='mobile-first-screen.css';document.head.appendChild(mobileFirstScreenStyles);

const header=document.querySelector('.site-header');const desktopNav=document.querySelector('.nav');if(header&&desktopNav){const menuButton=document.createElement('button');menuButton.className='mobile-menu-toggle';menuButton.type='button';menuButton.setAttribute('aria-label','Open menu');menuButton.setAttribute('aria-expanded','false');menuButton.innerHTML='<span></span><span></span><span></span>';const mobileNav=document.createElement('nav');mobileNav.className='mobile-nav';mobileNav.setAttribute('aria-label','Mobile navigation');desktopNav.querySelectorAll('a').forEach(link=>mobileNav.appendChild(link.cloneNode(true)));header.appendChild(menuButton);header.appendChild(mobileNav);const closeMenu=()=>{header.classList.remove('mobile-menu-open');menuButton.setAttribute('aria-expanded','false');menuButton.setAttribute('aria-label','Open menu');};menuButton.addEventListener('click',()=>{const willOpen=!header.classList.contains('mobile-menu-open');header.classList.toggle('mobile-menu-open',willOpen);menuButton.setAttribute('aria-expanded',String(willOpen));menuButton.setAttribute('aria-label',willOpen?'Close menu':'Open menu');});mobileNav.querySelectorAll('a').forEach(link=>link.addEventListener('click',closeMenu));document.addEventListener('keydown',event=>{if(event.key==='Escape')closeMenu();});}
